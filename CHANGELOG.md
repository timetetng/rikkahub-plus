# 更新日志

rikkahub 的个人分支（fork）。上游功能照常跟进，这里额外记录**本地工具链改造**的变更 ——
也就是「让模型在本机 / 容器里干活」的那部分（`env_*`、`file` 工具与执行通道）。
这些改动不碰聊天界面、后端协议与会话数据。

---

## [2.8.2] — 2026-09-16

`versionCode 185` · `versionName 2.8.2` · 全部改动集中在工具层

### 修复：写大文件会损坏内容（严重）

`env_write_file` / `env_edit_file` 写超过一个分块的内容时写坏文件，两个独立根因：

- **base64 分块错位**：载荷先被 `.chunked(76)` 折了行，写回时却按**字符**每 2048 切块 ——
  换行不参与 base64 的 4 字符对齐，每块尾巴都多出 2 个字符。表现按解码器分叉：
  GNU `base64 -d` 报 `invalid input` 并截断（实测 8945 B 的中文内容只写出 1516 B）；
  toybox 的 `base64 -d` **静默丢掉余数并返回 0**，于是工具报「写成功」而文件从断点起全是乱码。
  → 载荷不再折行、分块天然 4 对齐，并在写回后**校验字节数**（toybox 的静默成功只能这样拦）。
- **并发互相覆盖**：同一条消息里的两个 tool call 是并发执行的，两边都读到改动前的快照，
  后写的覆盖先写的（实测两个 patch 只有第二个生效）。此前怀疑是 FUSE 缓存，实为并发。
  → `file` 工具按 canonicalPath、`env` 工具按 `目标:路径` 加锁串行化。

### 修复

- `env_exec` 的输出截断对**单行超长**输出失效（整行原样吐出，12 万字符上限形同虚设）；
  现在至少保留该行尾部，并注明省略了多少。
- `readFileCapped` 按字节 seek 可能从 UTF-8 字符中间切开 → 丢掉第一个残缺行。
- 取消生成不再被吞：写入路径里用 `runCatching` 包 suspend 调用会把 `CancellationException`
  一起吃掉，用户点停止后仍会顺着回退分支继续写。

### 新增

- `file patch` / `env_edit_file` 支持 **`edits[]` 一次改多处**（对原文定位、不允许重叠；
  单条 `old_string` / `new_string` 写法保持兼容）。
- `file read` 行数 + 字节双限（2000 行 / 128 KB），截断时给出续读提示；文件上限 5 MB → 16 MB。
- `env_exec` 输出超 512 KB 时**完整落盘**，并把路径回给模型 —— 截断不再等于信息消失。

### 性能：大文件写入 1 跳搞定

- 新路径：app 先把内容写进自己的 external files（app 侧写文件不花 exec 往返），目标侧一条
  `cat "$s" > "$p"` 搬过去并自带字节校验 —— **任意大小 1 跳**。
  - 实测 23,872 B 中文：写入窗口 **1050 ms → 105 ms**（约 10 倍）
  - `root` 真机目标同样走这条（暂存文件生命周期 195 ms）
  - 目标看不见暂存文件（ssh 等）时自动退回分块路径，行为不退化
- 分块 base64 载荷上限 2048 → 7168 字符（实测 `droidspaces --name=X run sh -c '<cmd>'`
  整条命令上限 ≈ 8 KiB）。
- 分块路径：首块顺手做「建目录 + 截断」、末块顺手做「校验字节数」，24 KB 由 8 跳降到 6 跳。
- 接住 droidspaces 命令超限时的静默丢块：`rc=0`，只在 stdout 打一行 `daemon: bad request`。

### 与上游的关系

工具层之外与上游 2.8.2 一致。`versionCode` 比上游 2.8.2（184）大 1，因为有各自的出包。
